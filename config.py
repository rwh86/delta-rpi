# postgresql database, see README.md for schema definitions
db={
    'dbname':'delta-rpi',
    'user':'delta-rpi',
    'password':'(password)',
    'host':'localhost'}

# https://pvoutput.org/account.jsp
pvoutput={
    'apikey':'(apikey)',
    'systemid':'(systemid)'}
